---
import Layout from "../layouts/Layout.astro";
import SectionContainer from "../components/SectionContainer.astro";
---

<Layout
  title="Sistema de Inventarios PrintHub"
  description="Sistema de gestión de inventarios"
>
  <SectionContainer>
    <div id="inventory-app"></div>
  </SectionContainer>
</Layout>

<script>
  import InventoryDashboard from "../components/InventoryDashboard";
  import React from "react";
  import { createRoot } from "react-dom/client";

  const container = document.getElementById("inventory-app");
  if (container) {
    const root = createRoot(container);
    root.render(React.createElement(InventoryDashboard));
  }
</script>
